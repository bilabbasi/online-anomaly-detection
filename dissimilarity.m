function dyads = dissimilarity(H,x)
%------------------------------------------------------------------------%
%     Returns a matrix of dyads for some specificed dissimilarity metric.
% 
%     Parameters
%     ----------
%     H : Data set
%     x : New data
%
%     Returns
%     -------
%     dyads : Dyads with respect to H and x. 
%------------------------------------------------------------------------%

    %Include dyads function here. 
    %This code assumes that the dyads are structured in the following way:
    %   [d(X_1,X_2), d(X_1,X_3), ... , d(X_1,X_T),
    %   d(X_2,X_3),...,d(X_2,d(X_T),...,d(X_(T-1),X_T)]
    %Where d is the dissimilarity function.
    
    
    
end